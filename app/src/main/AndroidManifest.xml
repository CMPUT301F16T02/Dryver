<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ubertapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.NoTitleBar">
        <activity android:name=".Activities.ActivityOpeningPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ActivityLogin" />
        <activity android:name=".Activities.ActivityRegistration" />
        <activity android:name=".Activities.ActivitySelection" />
        <activity android:name=".Activities.ActivityDriver" />
        <activity android:name=".Activities.ActivityRiderRequest" />
        <activity
            android:name=".Activities.ActivityUserProfile"
            android:label="@string/title_activity_activity_user_profile" />
        <activity
            android:name=".Activities.ActivityRequestSelection"
            android:label="@string/title_activity_activity_request_selection"></activity>
    </application>

</manifest>